import sampleData from '../../sampleData'

const initState = sampleData.stories

export const stories = (state = initState, action) => {
  switch (action.type) {
    
    default:
      return state
  }
}