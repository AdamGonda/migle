import sampleData from '../../sampleData'

const initState = sampleData.sprints

export const sprint = (state = initState, action) => {
  switch (action.type) {
    
    default:
      return state
  }
}