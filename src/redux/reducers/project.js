import sampleData from '../../sampleData'

const initState = sampleData.projects

export const project = (state = initState, action) => {
  switch (action.type) {
    
    default:
      return state
  }
}
